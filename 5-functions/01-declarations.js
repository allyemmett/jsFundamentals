/*
    FUNCTIONS

        - functions are a chunk of code that perform a set or a set of actions when called (or invoked)
        - functions are reusable -  we can call them as many times and they can take in different sets of data 
*/
// function declaration
function hi() {
    console.log("hello");
}
hi(); // this is the invocation, without this function will not run
